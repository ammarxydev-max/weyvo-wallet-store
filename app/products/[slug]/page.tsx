import {notFound} from 'next/navigation';import Link from 'next/link';import {sql} from '@vercel/postgres';import {seedProducts} from '@/lib/database';
export const dynamic='force-dynamic';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){await seedProducts();const {slug}=await params;const {rows}=await sql`SELECT * FROM products WHERE slug=${slug} AND active=true LIMIT 1`;const p=rows[0];if(!p)return notFound();const images=(p.images||[]).length?p.images:['/hero.jpg'];return <main><div className="container productnav"><Link href="/" className="brand">WEYVO</Link><Link href="/">Shop</Link></div><section className="container product-page"><div className="product-gallery"><div className="product-main-image"><img src={images[0]} alt={p.name}/></div><div className="product-thumbs">{images.slice(0,5).map((src:string,i:number)=><img key={i} src={src} alt={`${p.name} ${i+1}`}/>)}</div></div><div className="product-info"><div className="eyebrow">WEYVO / {p.category}</div><h1>{p.name}</h1><div className="rating">★★★★★ <span>Premium everyday carry</span></div><div className="product-price">{p.price.toLocaleString()} EGP {p.compare_price&&<del>{p.compare_price.toLocaleString()} EGP</del>}</div><p className="lead">{p.description}</p><div className="buybox"><div><strong>Free delivery</strong><span>Cash on delivery available</span></div><Link className="btn" href={`/checkout?product=${encodeURIComponent(p.slug)}`}>Buy now</Link></div><div className="trust"><div><b>RFID protection</b><span>Added everyday security</span></div><div><b>Slim profile</b><span>Carry less, look better</span></div><div><b>Premium finish</b><span>Built for daily use</span></div></div></div></section><section className="container story"><h2>Designed around your everyday.</h2><p>Every detail of WEYVO is designed to make carrying your essentials simpler. Clean lines, premium materials and a practical layout give you a wallet that looks as good as it works.</p><div className="story-grid">{images.slice(0,3).map((src:string,i:number)=><img key={i} src={src} alt="WEYVO detail"/>)}</div></section></main>}
