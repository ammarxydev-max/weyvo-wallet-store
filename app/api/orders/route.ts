import {NextResponse} from 'next/server';import {sql} from '@vercel/postgres';import {ensureDb,seedProducts} from '@/lib/database';
const NFC_PRICE=150;
async function sendOrderEmail(order:any){const key=process.env.RESEND_API_KEY;const to=process.env.ADMIN_EMAIL;if(!key||!to)return;const from=process.env.RESEND_FROM||'WEYVO Orders <onboarding@resend.dev>';try{await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to,subject:`🛍️ New WEYVO Order ${order.orderNumber}`,html:`<div style="font-family:Arial,sans-serif;line-height:1.7"><h2>🛍️ New WEYVO Order</h2><p><b>Order:</b> ${order.orderNumber}</p><p><b>Product:</b> ${order.product}<br><b>Color:</b> ${order.color}<br><b>Quantity:</b> ${order.quantity}<br><b>Total:</b> ${order.total} EGP<br><b>NFC:</b> ${order.nfcEnabled?'YES (+150 EGP)':'NO'}</p>${order.nfcEnabled?`<hr><h3>NFC chip data</h3><p><b>Name:</b> ${order.nfcName}<br><b>Phone:</b> ${order.nfcPhone}<br><b>Email:</b> ${order.nfcEmail||'-'}<br><b>Job/Title:</b> ${order.nfcJob||'-'}<br><b>Company:</b> ${order.nfcCompany||'-'}<br><b>Website/Social:</b> ${order.nfcLink||'-'}<br><b>Extra:</b> ${order.nfcExtra||'-'}</p>`:''}<hr><p><b>Customer:</b> ${order.name}<br><b>Phone:</b> ${order.phone}<br><b>Governorate:</b> ${order.governorate}<br><b>Area:</b> ${order.area}<br><b>Address:</b> ${order.address}<br><b>Notes:</b> ${order.notes||'-'}</p></div>`})});}catch(e){console.error('Order email failed',e)}}
export async function POST(req:Request){try{const b=await req.json();const{name,phone,governorate,area,address,notes='',color='Brown',quantity=1,productSlug='weyvo-smart-wallet',nfcEnabled=false,nfcData={}}=b;if(!name||!phone||!governorate||!area||!address)return NextResponse.json({error:'Please complete all required fields.'},{status:400});const qty=Number(quantity);if(!Number.isInteger(qty)||qty<1||qty>20)return NextResponse.json({error:'Invalid quantity.'},{status:400});if(nfcEnabled&&(!nfcData.name||!nfcData.phone))return NextResponse.json({error:'Please complete the required NFC information.'},{status:400});await seedProducts();const{rows}=await sql`SELECT name,price FROM products WHERE slug=${productSlug} AND active=true LIMIT 1`;if(!rows[0])return NextResponse.json({error:'Product not found.'},{status:404});const product=rows[0];const basePrice=Number(product.price);const unitPrice=basePrice+(nfcEnabled?NFC_PRICE:0);const total=qty*unitPrice;await ensureDb();const orderNumber='WYV-'+new Date().toISOString().slice(0,10).replaceAll('-','')+'-'+Math.floor(100000+Math.random()*900000);const nfcText=nfcEnabled?JSON.stringify(nfcData):'';await sql`INSERT INTO orders(order_number,product,color,quantity,unit_price,total,customer_name,phone,governorate,area,address,notes,nfc_enabled,nfc_data) VALUES(${orderNumber},${product.name},${color},${qty},${unitPrice},${total},${name},${phone},${governorate},${area},${address},${notes},${Boolean(nfcEnabled)},${nfcText})`;await sendOrderEmail({orderNumber,product:product.name,color,quantity:qty,total,name,phone,governorate,area,address,notes,nfcEnabled,nfcName:nfcData.name,nfcPhone:nfcData.phone,nfcEmail:nfcData.email,nfcJob:nfcData.job,nfcCompany:nfcData.company,nfcLink:nfcData.link,nfcExtra:nfcData.extra});const msg=`New WEYVO Order%0AOrder: ${orderNumber}%0AProduct: ${product.name}%0AColor: ${color}%0AQty: ${qty}%0ATotal: ${total} EGP%0ANFC: ${nfcEnabled?'YES (+150 EGP)':'NO'}%0AName: ${name}%0APhone: ${phone}%0AGovernorate: ${governorate}%0AArea: ${area}%0AAddress: ${address}%0ANotes: ${notes}`;return NextResponse.json({orderNumber,total,whatsappUrl:`https://wa.me/${process.env.WHATSAPP_NUMBER||'201067515979'}?text=${msg}`})}catch(e){console.error(e);return NextResponse.json({error:'Database is not configured or the order could not be saved.'},{status:500})}}
