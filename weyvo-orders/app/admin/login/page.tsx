'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [u,setU]=useState(''),[p,setP]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();async function go(e:any){e.preventDefault();setLoading(true);const x=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});if(x.ok)r.push('/admin');else{const d=await x.json();setErr(d.error||'Login failed')}setLoading(false)}return <main className="login"><form className="loginbox" onSubmit={go}><div className="brand">WEYVO</div><h1>Orders Dashboard</h1><p className="muted">Admin access</p><div className="field"><label>Username</label><input type="text" autoComplete="username" required value={u} onChange={e=>setU(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" autoComplete="current-password" required value={p} onChange={e=>setP(e.target.value)}/></div>{err&&<p style={{color:'#a33'}}>{err}</p>}<button className="btn" disabled={loading}>{loading?'Checking…':'Sign in'}</button></form></main>}
