import { sql } from '@vercel/postgres';
const connectionString=process.env.POSTGRES_URL||process.env.STORAGE_URL||process.env.DATABASE_URL;if(connectionString&&!process.env.POSTGRES_URL)process.env.POSTGRES_URL=connectionString;
export const BUNDLE_DISCOUNTS={2:0.1102,3:0.1831};export function getBundleDiscount(quantity:number){return quantity>=3?BUNDLE_DISCOUNTS[3]:quantity===2?BUNDLE_DISCOUNTS[2]:0;}
export async function ensureDb(){if(!process.env.POSTGRES_URL)throw new Error('Database connection is missing.');await sql`CREATE TABLE IF NOT EXISTS orders (id BIGSERIAL PRIMARY KEY,order_number TEXT UNIQUE NOT NULL,product TEXT NOT NULL,color TEXT NOT NULL,quantity INTEGER NOT NULL CHECK(quantity>0),unit_price INTEGER NOT NULL,total INTEGER NOT NULL,customer_name TEXT NOT NULL,phone TEXT NOT NULL,governorate TEXT NOT NULL,area TEXT NOT NULL,address TEXT NOT NULL,notes TEXT DEFAULT '',status TEXT NOT NULL DEFAULT 'New',nfc_enabled BOOLEAN NOT NULL DEFAULT false,nfc_data TEXT DEFAULT '',discount INTEGER NOT NULL DEFAULT 0,payment_method TEXT NOT NULL DEFAULT 'cod',payment_proof TEXT DEFAULT '',payment_status TEXT NOT NULL DEFAULT 'Pending',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS nfc_enabled BOOLEAN NOT NULL DEFAULT false`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS nfc_data TEXT DEFAULT ''`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS discount INTEGER NOT NULL DEFAULT 0`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_method TEXT NOT NULL DEFAULT 'cod'`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_proof TEXT DEFAULT ''`;await sql`ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_status TEXT NOT NULL DEFAULT 'Pending'`;await sql`CREATE TABLE IF NOT EXISTS products (id BIGSERIAL PRIMARY KEY,name TEXT NOT NULL,slug TEXT UNIQUE NOT NULL,description TEXT DEFAULT '',price INTEGER NOT NULL,compare_price INTEGER,images TEXT[] DEFAULT ARRAY[]::TEXT[],category TEXT DEFAULT 'Wallets',featured BOOLEAN DEFAULT false,active BOOLEAN DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());`;await sql`ALTER TABLE products ADD COLUMN IF NOT EXISTS price INTEGER NOT NULL DEFAULT 449`;await sql`CREATE TABLE IF NOT EXISTS product_reviews (id BIGSERIAL PRIMARY KEY,product_id BIGINT REFERENCES products(id) ON DELETE CASCADE,customer_name TEXT NOT NULL,rating INTEGER NOT NULL CHECK(rating BETWEEN 1 AND 5),review TEXT NOT NULL,verified BOOLEAN NOT NULL DEFAULT false,global_review_id TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());`;await sql`ALTER TABLE product_reviews ADD COLUMN IF NOT EXISTS global_review_id TEXT`;await sql`CREATE TABLE IF NOT EXISTS app_settings (key TEXT PRIMARY KEY,value TEXT NOT NULL);`;const{rows}=await sql`SELECT value FROM app_settings WHERE key='wallet_price_449_migrated' LIMIT 1`;if(!rows.length){await sql`UPDATE products SET price=449,updated_at=NOW() WHERE price IN (699,649,499)`;await sql`INSERT INTO app_settings(key,value) VALUES('wallet_price_449_migrated','449') ON CONFLICT(key) DO NOTHING`;}}
export async function seedProducts(){await ensureDb();const{rows}=await sql`SELECT COUNT(*)::int AS count FROM products`;if(Number(rows[0]?.count)>0)return;const products:Array<{name:string;slug:string;description:string;price:number;comparePrice:number;images:string[];featured:boolean}>=[{name:'WEYVO Smart Wallet',slug:'weyvo-smart-wallet',description:'Minimal everyday wallet with a premium finish, RFID protection and smart contact sharing.',price:449,comparePrice:699,images:['/hero.jpg','/open.jpg','/cards.jpg','/nfc.jpg'],featured:true},{name:'WEYVO Brown Edition',slug:'weyvo-brown-edition',description:'Classic brown finish for a refined everyday carry.',price:449,comparePrice:699,images:['/brown.png','/hero.jpg'],featured:false},{name:'WEYVO Black Edition',slug:'weyvo-black-edition',description:'Stealth black finish with a clean modern profile.',price:449,comparePrice:699,images:['/black.png','/hero.jpg'],featured:false},{name:'WEYVO Beige Edition',slug:'weyvo-beige-edition',description:'Soft beige finish designed for a premium minimal look.',price:449,comparePrice:699,images:['/beige.png','/hero.jpg'],featured:false},{name:'WEYVO Blue Edition',slug:'weyvo-blue-edition',description:'Deep blue finish designed for a distinctive everyday carry.',price:449,comparePrice:699,images:['/blue.png','/hero.jpg'],featured:false}];for(const p of products){const imagesJson=JSON.stringify(p.images);await sql`INSERT INTO products(name,slug,description,price,compare_price,images,featured) VALUES(${p.name},${p.slug},${p.description},${p.price},${p.comparePrice},ARRAY(SELECT jsonb_array_elements_text(${imagesJson}::jsonb)),${p.featured}) ON CONFLICT(slug) DO NOTHING`;}}
