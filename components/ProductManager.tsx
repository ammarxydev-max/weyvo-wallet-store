'use client';import{useEffect,useState}from'react';type P={id?:number,name:string,slug:string,description:string,price:number,compare_price?:number|null,images:string[],category:string,featured:boolean,active:boolean};const empty:P={name:'',slug:'',description:'',price:699,compare_price:1299,images:[],category:'Wallets',featured:false,active:true};export default function ProductManager(){const[items,setItems]=useState<P[]>([]),[form,setForm]=useState<P>(empty),[editing,setEditing]=useState<number|null>(null),[image,setImage]=useState('');const load=async()=>{const r=await fetch('/api/admin/products');if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const save=async()=>{const r=await fetch('/api/admin/products',{method:editing?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,id:editing})});if(!r.ok)return alert('Could not save product');setForm(empty);setEditing(null);load()};const remove=async(id:number)=>{if(confirm('Delete this product?')){await fetch('/api/admin/products?id='+id,{method:'DELETE'});load()}};return <div className="product-manager"><div className="manager-head"><div><h2>Products</h2><p className="muted">Add products, prices and image galleries.</p></div><button className="btn" onClick={()=>{setForm(empty);setEditing(null)}}>+ Add product</button></div><div className="product-form"><h3>{editing?'Edit product':'New product'}</h3><div className="grid"><div className="field"><label>Name</label><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>Slug</label><input value={form.slug} placeholder="auto if empty" onChange={e=>setForm({...form,slug:e.target.value})}/></div><div className="field"><label>Price (EGP)</label><input type="number" value={form.price} onChange={e=>setForm({...form,price:Number(e.target.value)})}/></div><div className="field"><label>Compare price</label><input type="number" value={form.compare_price||''} onChange={e=>setForm({...form,compare_price:e.target.value?Number(e.target.value):null})}/></div><div className="field"><label>Category</label><input value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></div><div className="field full"><label>Description</label><textarea rows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div className="field full"><label>Image URL</label><div className="inline"><input value={image} placeholder="https://... or /image.jpg" onChange={e=>setImage(e.target.value)}/><button className="btn secondary" onClick={()=>{if(image){setForm({...form,images:[...form.images,image]});setImage('')}}}>Add image</button></div></div><div className="image-list full">{form.images.map((src,i)=><div key={i}><img src={src} alt=""/><button onClick={()=>setForm({...form,images:form.images.filter((_,x)=>x!==i)})}>×</button></div>)}</div></div><div className="checks"><label><input type="checkbox" checked={form.featured} onChange={e=>setForm({...form,featured:e.target.checked})}/> Featured</label><label><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/> Published</label></div><button className="btn" onClick={save}>{editing?'Save changes':'Create product'}</button></div><div className="product-list">{items.map(p=><div className="product-row" key={p.id}><img src={p.images?.[0]||'/hero.jpg'} alt=""/><div className="product-row-info"><b>{p.name}</b><span>{p.price.toLocaleString()} EGP · {p.category}</span></div><button className="btn secondary" onClick={()=>{setForm({...p,images:p.images||[]});setEditing(p.id||null)}}>Edit</button><button className="danger" onClick={()=>remove(p.id!)}>Delete</button></div>)}</div></div>}
